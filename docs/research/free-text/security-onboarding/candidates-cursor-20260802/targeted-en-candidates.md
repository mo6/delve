# When It Is Written For You — en candidate answers

Source: `docs/research/free-text/security-onboarding/targeted-en.md`

## Candidate correct answers

1. **"the transaction"** — why this should ACCEPT: Direct paraphrase of Grigor's canonical line ("The transaction is left") and the first accept entry.
2. **"check what is being asked"** — why this should ACCEPT: Restates "Look at what is being asked" from the lesson without copying accept-list wording; same meaning as verifying the request.
3. **"use a second channel"** — why this should ACCEPT: Short form of the lesson's defence (phone you already had, walk to the desk); matches "verify through a second channel".
4. **"call them on a number you already have"** — why this should ACCEPT: Concrete second-channel behaviour from the lesson; a learner answering with the method rather than the label "transaction" is still correct.
5. **"verify the money movement"** — why this should ACCEPT: Casual register for "Money moving somewhere new"; same idea as verifying the transaction.
6. **"don't trust who is asking, check the ask"** — why this should ACCEPT: Echoes "Never mind who is asking. Look at what is being asked"; meaning-aligned without accept-list keywords.
7. **"bank detail changes"** — why this should ACCEPT: Partial restatement of the high-stakes request types the lesson lists; if the learner means "verify those", it should count.
8. **"confirm the request out of band"** — why this should ACCEPT: Formal synonym for second-channel verification; a non-native or security-aware phrasing of the same defence.

## Candidate wrong answers

1. **"your LinkedIn profile"** — why this should REJECT: Matches the reject theme (visibility / online profile); the lesson says you cannot undo public info. Flag: unlikely to be accepted.
2. **"how much the attacker knows about you"** — why this should REJECT: Explicit reject-list idea; reconnaissance is not the defendable step.
3. **"the sender's domain"** — why this should REJECT: Lesson says domain/tone/request can all look right in BEC; verifying the domain misses the point. Flag: a lenient grader might accept if it thinks "verify something".
4. **"delete your public profile"** — why this should REJECT: Misconception the explanation warns against; undoing visibility is impossible and not the ask.
5. **"was I expecting this"** — why this should REJECT: Correct for the links/attachments room, wrong here; tests cross-room pattern-matching.
6. **"enable MFA"** — why this should REJECT: Adjacent vault topic, not what this question asks.
7. **"the spelling of the email"** — why this should REJECT: Bulk-phishing tell; spear phishing is built to pass those checks.
8. **"purple elephant"** — why this should REJECT: Off-topic floor test.

## Quality assessment

- Question clarity: Clear; "instead" of undoing public information points at the transaction/request.
- Lesson/question alignment: Strong. Grigor's speech ends on "The transaction is left" and second-channel verification; accept list tracks that closely.
- Accept-list coverage: Good range (transaction, request, second channel). May miss "call them back", "out of band", "verify bank details" under keyword floor if those exact substrings are absent.
- Reject-list false-positive risk: Low. Reject phrases are about visibility/profile knowledge, unlikely to appear inside a correct "verify the transaction" answer.
- Explanation consistency: Consistent; explanation restates that the request/transaction via a second channel is the defence.

## Suggested refinements

- Add accept phrases: `out of band`, `call them back`, `second channel`, `what is being asked`.
- Optional reject: `the domain`, `the sender` (common wrong focuses the lesson already dismisses).
